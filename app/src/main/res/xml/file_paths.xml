<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Cache directory for temporary camera images -->
    <cache-path
        name="cache"
        path="." />
    
    <!-- Files directory if needed for persistent images -->
    <files-path
        name="files"
        path="." />
</paths>
